<footer class="site-footer">
    <div class="footer-grid">
        <!-- Brand Column -->
        <div class="footer-brand">
            <div class="footer-logo">
                <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcT7QnjcTUOpzRq05tWsyO5NYOmThuRS-5estg&s" alt="Logo" style="height: 36px; width: 36px; border-radius: 10px; object-fit: cover; box-shadow: 0 4px 14px rgba(79,142,247,.3);" />
                <span class="footer-logo-text">Shop<span>Wave</span></span>
            </div>
            <p class="footer-desc">Elevating your lifestyle with premium products and a seamless shopping experience. Join the wave today.</p>
            <div class="footer-socials">
                <a href="#" class="social-link"><i class="bi bi-facebook"></i></a>
                <a href="#" class="social-link"><i class="bi bi-twitter-x"></i></a>
                <a href="#" class="social-link"><i class="bi bi-instagram"></i></a>
                <a href="#" class="social-link"><i class="bi bi-linkedin"></i></a>
            </div>
        </div>

        <!-- Shop Links -->
        <div class="footer-col">
            <h5 class="footer-heading">Shop</h5>
            <ul class="footer-links">
                <li><a href="/products">All Products</a></li>
                <li><a href="#">Featured</a></li>
                <li><a href="#">New Arrivals</a></li>
                <li><a href="#">Sales</a></li>
            </ul>
        </div>

        <!-- Support Links -->
        <div class="footer-col">
            <h5 class="footer-heading">Support</h5>
            <ul class="footer-links">
                <li><a href="mailto:support@shopwave.pk">Contact Support</a></li>
                <li><a href="#">Shipping Policy</a></li>
                <li><a href="#">Returns & Refunds</a></li>
                <li><a href="#">FAQs</a></li>
            </ul>
        </div>

        <!-- Newsletter -->
        <div class="footer-col">
            <h5 class="footer-heading">Newsletter</h5>
            <p class="footer-desc" style="margin-bottom:12px;">Subscribe to get special offers and once-in-a-lifetime deals.</p>
            <div class="newsletter-form">
                <input type="email" class="form-control" placeholder="Your Email Address" />
                <button class="btn btn-primary btn-full" style="margin-top:10px;">
                    <i class="bi bi-send"></i> Subscribe
                </button>
            </div>
        </div>
    </div>

    <div class="footer-divider"></div>

    <div class="footer-bottom">
        <p class="footer-copy">© 2024 ShopWave Inc. All rights reserved.</p>
        <div class="footer-legal">
            <a href="#">Privacy Policy</a>
            <a href="#">Terms of Service</a>
        </div>
    </div>
</footer>

<style>
    .site-footer {
        background: var(--surface);
        border-top: 1px solid var(--border);
        padding: 48px 32px 24px;
        margin-top: 40px;
    }

    .footer-grid {
        display: grid;
        grid-template-columns: 1.5fr 1fr 1fr 1.5fr;
        gap: 40px;
        max-width: 1200px;
        margin: 0 auto;
    }

    .footer-logo {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 16px;
    }
    .footer-logo-mark {
        width: 36px; height: 36px; border-radius: 10px;
        background: linear-gradient(135deg, var(--primary), var(--purple));
        display: grid; place-items: center;
        font-family: var(--font-display); font-weight: 800; font-size: .85rem; color: var(--text);
        box-shadow: 0 4px 14px rgba(79,142,247,.3);
    }
    .footer-logo-text {
        font-family: var(--font-display); font-size: 1.2rem; font-weight: 800; color: var(--text);
    }
    .footer-logo-text span { color: var(--primary); }

    .footer-desc {
        font-size: .85rem; color: var(--text-muted); line-height: 1.7;
        max-width: 300px;
    }

    .footer-socials {
        display: flex; gap: 10px; margin-top: 16px;
    }
    .social-link {
        width: 38px; height: 38px; border-radius: 50%;
        display: grid; place-items: center;
        background: var(--panel); border: 1px solid var(--border);
        color: var(--text-2); text-decoration: none;
        transition: var(--t); font-size: .9rem;
    }
    .social-link:hover {
        background: var(--primary); color: var(--text);
        transform: translateY(-3px);
        box-shadow: 0 4px 12px rgba(79,142,247,.3);
    }

    .footer-heading {
        font-family: var(--font-display); font-weight: 700; font-size: .95rem;
        color: var(--text); margin-bottom: 16px;
    }

    .footer-links {
        list-style: none; padding: 0; margin: 0;
    }
    .footer-links li { margin-bottom: 10px; }
    .footer-links a {
        color: var(--text-muted); text-decoration: none;
        font-size: .85rem; transition: var(--t);
        display: inline-flex; align-items: center; gap: 6px;
    }
    .footer-links a::before {
        content: ''; width: 4px; height: 4px; border-radius: 50%;
        background: var(--border-light); transition: var(--t);
    }
    .footer-links a:hover {
        color: var(--primary);
    }
    .footer-links a:hover::before {
        background: var(--primary);
    }

    .footer-divider {
        height: 1px; background: var(--border); margin: 32px 0 20px;
        max-width: 1200px; margin-left: auto; margin-right: auto;
    }

    .footer-bottom {
        display: flex; justify-content: space-between; align-items: center;
        max-width: 1200px; margin: 0 auto;
    }
    .footer-copy {
        font-size: .78rem; color: var(--text-muted); margin: 0;
    }
    .footer-legal {
        display: flex; gap: 20px;
    }
    .footer-legal a {
        font-size: .78rem; color: var(--text-muted); text-decoration: none; transition: var(--t);
    }
    .footer-legal a:hover { color: var(--primary); }

    @@media (max-width: 768px) {
        .footer-grid { grid-template-columns: 1fr 1fr; gap: 28px; }
    }
    @@media (max-width: 480px) {
        .footer-grid { grid-template-columns: 1fr; }
        .footer-bottom { flex-direction: column; gap: 12px; text-align: center; }
    }
</style>

